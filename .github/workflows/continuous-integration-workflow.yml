name: Build website
on:
  push:
    branches:
      - dev

jobs:
  build:
    name: Build website
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v1

    - name: Build and deploy
      uses: flachware/github-pages-deploy-action@master

